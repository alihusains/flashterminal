# skills/

Reusable Claude Code skills extracted from work on this repo. Each subdirectory is a self-contained skill (`SKILL.md` + `Workflows/`) also installed to the user's global skill library (`~/.claude/skills/`).

- **CiForensicRepair** — diagnose and repair a flaky/broken GitHub Actions CI using evidence from actual run history; includes a workflow for evidence-based performance/benchmark CI gate design. Extracted from the two CI-repair phases in `phases/Forensic-repair.md`, `phases/Forensic-repair-2-eventbus.md`.
